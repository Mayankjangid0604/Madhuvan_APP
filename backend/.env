PORT=5001
JWT_SECRET=super_secret_admin_key
JWT_EXPIRES_IN=1d
NODE_ENV=development

# MSG91 (SMS + Email + WhatsApp) is configured from Settings -> MSG91 Configuration
# in the app (stored in the database), not from environment variables here.

# Google Drive Configuration
GOOGLE_DRIVE_FOLDER_ID=your_folder_id

/**
 * Server Entry Point
 * 
 * This file simply imports and runs the main app.
 * 
 * Usage:
 *   node server.js
 *   OR
 *   node app.js (directly)
 */

const server = require("./app");

module.exports = server;
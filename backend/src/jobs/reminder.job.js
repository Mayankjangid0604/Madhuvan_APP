/**
 * Reminder Job
 * 
 * This job is disabled. Fee reminders are handled by:
 * - cron/feeReminder.cron.js (scheduled reminders)
 * - Manual reminders via API
 */

// Job disabled - reminders handled elsewhere
module.exports = {};